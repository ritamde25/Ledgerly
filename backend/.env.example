# Ledgerly YOLO Backend
# Copy this file to `.env` (the real `.env` is gitignored).

# Server
PORT=5000

# Optional local model path override.
# If this file path does not exist, backend auto-downloads the project's
# Hugging Face model and caches it at backend/.model_cache/best.pt.
MODEL_PATH=best.pt
